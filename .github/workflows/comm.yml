name: common_steps

on:
  pull_request:
    branches: [ love ]

permissions:
      id-token: write
      contents: read

jobs:

  echo:
    runs-on: ubuntu-latest
    steps:
      - name: git clone the repository
        uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: where am i
        run: |
          echo "PR!!"
