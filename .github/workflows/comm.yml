name: common_steps

on:
  pull_request:
    branches: [ love ]

permissions:
      id-token: write
      contents: read

jobs:

  echo:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: where am i
        run: |
            echo "PR!!"
