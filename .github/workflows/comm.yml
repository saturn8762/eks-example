name: common_steps
on:
  workflow_call:
    inputs:
      env:
        type: string
        required: true
    
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: where am i
        run: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            echo "PR!!"
          elif [ "${{ github.event_name }}" == "push" ]; then
            echo "Push!!"
          fi
